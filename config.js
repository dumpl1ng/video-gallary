
// specify the gallery width and height
var galleryWidth = ''; 
var galleryHeight = '';

// the scrolling direction of auto scroll, "vertical"/"horizontal"
var scrollDirection = "horizontal";

// number of videos per showcase
var numVideoPerBlock = 4;

// specify whether to play the video inside the video block
var playVideoInTheBlock = true;

// if it is playing right now
var isPlaying = false;

// specify the alignment direction of the showcases
var alignment = "horizontal";

// specify the length of the interval that each slide stays
var slideStayInterval = 5000;

// specify the speed of the sliding animation
var animationSpeedInterval = 1000;